version: "3.9"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8000/api/v1/
        VITE_APP_APPLICATION_ID: ""
        VITE_APP_API_KEY: ""
        VITE_APP_SERVER_KEY: ""
    ports:
      - "3000:3000"
    restart: always
